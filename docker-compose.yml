services:
  user-service:
    build: ./user-service
    ports:
      - "8000:8000"
    restart: unless-stopped

  order-service:
    build: ./order-service
    ports:
      - "8001:8001"
    restart: unless-stopped
    depends_on:
      - user-service